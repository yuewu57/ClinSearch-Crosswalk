# ClinSearch-Crosswalk browser implementation

This directory will contain the browser-side TypeScript implementation. It is being developed against frozen parity fixtures derived from the Python reference implementation.
